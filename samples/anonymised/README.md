# Anonymised samples

This folder accepts synthetic or deliberately anonymised fixtures only.

Allowed fixture suffixes are `.fixture.json` and `.fixture.csv`. Never copy a client workbook and rename it. Build fixtures from fictional values designed to exercise a stated test case.

